import type { NextConfig } from "next"

const nextConfig: NextConfig = {
    images: {
        domains: ["www.google.com", "wallpaper.forfun.com"], // Add the hostname here
    },
    /* config options here */
}

export default nextConfig
